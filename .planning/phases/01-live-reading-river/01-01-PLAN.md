---
phase: 01-live-reading-river
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - index.html
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - src/main.ts
  - src/config.ts
  - src/feed/types.ts
  - src/feed/fetchFeed.ts
  - src/feed/parseRss.ts
  - src/feed/loadRiver.ts
  - src/ui/render.ts
  - src/ui/river.ts
  - src/ui/piece.ts
  - src/ui/loading.ts
  - src/style.css
  - README.md
  - CHANGELOG.md
  - .gitignore
autonomous: false
requirements:
  - FEED-01
  - FEED-02
  - RIVER-02
  - RIVER-03
  - REPO-01
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Visitor runs the local Vite site and sees pieces from the live feed, newest first"
    - "Each piece shows a title, a date, body text from the item description, and a dergigi.com permalink"
    - "A new thoughts-tagged post on dergigi.com can appear here without rebuilding this site"
    - "The repo is SemVer 0.1.0 with a Keep a Changelog file and a README that says how to run"
    - "The river never invents items; the live feed is the only source"
  artifacts:
    - path: src/config.ts
      provides: FEED_URL and PUBLISHER_ORIGIN constants
      contains: FEED_URL
    - path: src/feed/fetchFeed.ts
      provides: runtime GET of the live RSS URL
      exports: ["fetchFeed"]
    - path: src/feed/parseRss.ts
      provides: RSS 2.0 item parse via DOMParser
      exports: ["parseRss"]
    - path: src/feed/loadRiver.ts
      provides: pipeline orchestrator returning RiverState
      exports: ["loadRiver"]
    - path: src/ui/river.ts
      provides: newest-first river render
    - path: README.md
      provides: how to run npm run dev
    - path: CHANGELOG.md
      provides: Keep a Changelog starting at 0.1.0
      contains: 0.1.0
  key_links:
    - from: src/main.ts
      to: src/feed/loadRiver.ts
      via: boot calls loadRiver after showing loading
      pattern: loadRiver
    - from: src/feed/fetchFeed.ts
      to: src/config.ts
      via: fetch uses FEED_URL
      pattern: FEED_URL
    - from: src/ui/piece.ts
      to: item link field
      via: permalink href is the feed item link
      pattern: canonicalUrl
---

## Phase Goal

**As a** visitor, **I want to** read the live thoughts river, **so that** a new tagged post appears without a redeploy.

<objective>
Walking skeleton: official Vite vanilla-ts app that fetches the live RSS in the browser, parses items, and renders one river. Repo conventions land in the same wave.

Purpose: Prove D-01 (runtime fetch) through D-02 / D-04 / D-06 / D-10 end-to-end before sanitize, empty-state copy, and paper CSS.
Output: Runnable `npm run dev` site plus SemVer / changelog / README.
</objective>

<execution_context>
@/Users/gigi/Development/thinking-in-public/.cursor/gsd-core/workflows/execute-plan.md
@/Users/gigi/Development/thinking-in-public/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-live-reading-river/01-CONTEXT.md
@.planning/phases/01-live-reading-river/SKELETON.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@PRODUCT.md
</context>

## must_haves

Truths are in frontmatter. Prohibitions for this plan:

- Do not fetch the feed during `vite build` or bake items into `dist/` (D-01).
- Do not add a CMS, markdown copies, or a local editorial store.
- Do not invent river items for demos or screenshots.
- Do not add React, Astro, Next, or Tailwind (D-10).
- Do not add cards, heroes, teasers, or pagination (D-04).
- Do not install DOMPurify here; that is plan 01-02 (D-03).
- Do not run Impeccable polish (Phase 2 / READ-02).
- Do not write a GSD UI-SPEC.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Confirm one-way door: runtime feed fetch</name>
  <decision>Honor locked D-01: the browser GETs the live thoughts RSS at runtime. A build-time snapshot would freeze the river and break the core value.</decision>
  <context>D-01 is already locked in 01-CONTEXT.md. This gate records the one-way door before the tracer walks through it. Undo later means every later slice inherits a baked feed and new tagged posts stay invisible until redeploy.</context>
  <options>
    <option id="runtime-fetch">
      <name>Proceed with runtime fetch (D-01)</name>
      <pros>New thoughts-tagged posts appear without redeploying this site. Matches the core value.</pros>
      <cons>The page needs the network. If CORS later closes, empty state shows until a same-origin fallback exists.</cons>
    </option>
    <option id="stop">
      <name>Stop before scaffolding</name>
      <pros>Does not walk through the door.</pros>
      <cons>Leaves no reading site. Do not choose a build-time snapshot; that path is forbidden.</cons>
    </option>
  </options>
  <resume-signal>Select: runtime-fetch or stop</resume-signal>
</task>

<task type="tracer">
  <name>End-to-end live river — scaffold, fetch, parse, render</name>
  <reversibility rating="one-way">A build-time snapshot would freeze the river and break the core value; D-01 is already locked in 01-CONTEXT.md so this task records the door rather than re-asking it.</reversibility>
  <precondition>curl -sI https://dergigi.com/thoughts.xml reports HTTP 200 (public RSS; no credentials)</precondition>
  <files>package.json, index.html, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, src/main.ts, src/config.ts, src/feed/types.ts, src/feed/fetchFeed.ts, src/feed/parseRss.ts, src/feed/loadRiver.ts, src/ui/render.ts, src/ui/river.ts, src/ui/piece.ts, src/ui/loading.ts, src/style.css, .gitignore</files>
  <read_first>.planning/phases/01-live-reading-river/01-CONTEXT.md, .planning/research/ARCHITECTURE.md, .planning/research/STACK.md, PRODUCT.md, .planning/phases/01-live-reading-river/SKELETON.md</read_first>
  <action>
Scaffold the official Vite vanilla-ts template into this repo root per D-10. The directory already holds .git, .planning, .cursor, AGENTS.md, and PRODUCT.md. If create-vite refuses a non-empty folder, scaffold into a temp directory and copy only the Vite app files (index.html, src/, vite.config.ts, tsconfig*.json, package.json, .gitignore, public/). Never overwrite PRODUCT.md, AGENTS.md, .planning/, or .cursor/.

Keep TypeScript on the 6.x line the template pins (~6.0.2). Scripts must be dev (vite), build (tsc && vite build), and preview (vite preview). Delete the template counter demo, default Vite marketing markup, and unused SVG assets.

Add src/config.ts with FEED_URL set to the live thoughts RSS on dergigi.com and PUBLISHER_ORIGIN set to https://dergigi.com. Optional FALLBACK_FEED_URL may exist as a constant; do not add a Vite server.proxy and do not add a Worker while CORS remains open.

Add src/feed/types.ts with Piece (title, publishedAt as Date or null, canonicalUrl, bodyText for this skeleton) and RiverState as the union loading | ready (pieces) | empty (reason: no-items | fetch-failed | parse-failed | cors-failed).

Implement fetchFeed per D-01: browser fetch of FEED_URL at runtime, response.text(), map network or non-OK to a typed failure. Do not call fetch from vite.config.ts, from a Vite plugin, or from any build script.

Implement parseRss per D-02: DOMParser with application/xml, reject parsererror, read each item title, pubDate, link, and description via textContent (one XML decode; no extra entity decoder). Sort newest first by Date.parse of pubDate even if today's feed is already ordered (D-04). Keep items with missing or unparsable pubDate; do not drop them.

Implement loadRiver: fetch → parse → ready or empty. Do not sanitize in this plan (D-03 is 01-02).

Boot in src/main.ts: render loading (quiet one-line status is enough; Claude's discretion), await loadRiver, render the result. src/ui/render.ts switches on RiverState. src/ui/river.ts is one long column, newest first, single page (D-04). src/ui/piece.ts shows title, a date string, description body, and a permalink whose href is the item link on dergigi.com (D-06). Title, date, and permalink are text nodes. For this skeleton only, put description into the body node with textContent so the insert sink exists without opening an HTML sink; plan 01-02 replaces that body with sanitized markup.

index.html title is Thinking in Public. Mount #app. Channel title on the page may be the string Thinking in Public (D-08). Empty reason may render as a blank or one quiet line; designed empty copy is 01-02 (D-07).

Do not add React or other UI frameworks. Do not add a local editorial store. Do not invent river items.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>test "$(grep -vE '^\s*(//|/\*|\*)' src/config.ts | grep -c 'FEED_URL')" -ge 1</automated>
    <automated>test "$(grep -vE '^\s*(//|/\*|\*)' src/feed/fetchFeed.ts | grep -c 'FEED_URL')" -ge 1</automated>
    <automated>test "$(grep -vE '^\s*(//|/\*|\*)' src/feed/parseRss.ts | grep -c 'DOMParser')" -ge 1</automated>
    <automated>test "$(grep -vE '^\s*(//|/\*|\*)' src/feed/parseRss.ts | grep -c 'textContent')" -ge 1</automated>
    <automated>test "$(grep -vE '^\s*(//|/\*|\*)' src/ui/piece.ts | grep -c 'canonicalUrl')" -ge 1</automated>
    <automated>test "$(grep -vE '^\s*(//|/\*|\*)' vite.config.ts | grep -c 'fetch(')" -eq 0</automated>
    <automated>test ! -d content</automated>
    <human-check>Run npm run dev, open the local URL, confirm live feed titles appear newest first and each permalink goes to dergigi.com.</human-check>
  </verify>
  <acceptance_criteria>
    - Official Vite vanilla-ts app builds with tsc && vite build (D-10).
    - Browser code fetches FEED_URL at runtime; Vite config has no fetch (D-01, FEED-01).
    - parseRss reads title, pubDate, link, description via DOMParser textContent (D-02, FEED-02).
    - River is one column, newest first, single page (D-04, RIVER-02).
    - Each piece permalink is the feed item link (D-06, RIVER-03).
    - Title, date, and permalink are text nodes; skeleton body uses textContent.
    - No local editorial store; no invented items.
  </acceptance_criteria>
  <done>Local npm run dev shows the live feed as one river; build stays a shell that fetches at runtime.</done>
</task>

<task type="auto">
  <name>Repo conventions: SemVer, changelog, README</name>
  <files>package.json, README.md, CHANGELOG.md</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md</read_first>
  <action>
Set package.json name to thinking-in-public and version to 0.1.0 per D-11. Keep the Vite scripts from the tracer.

Write CHANGELOG.md in Keep a Changelog format. Open with Unreleased, then 0.1.0 dated today (2026-08-19). Under Added, describe a local Vite reading site that loads the live thoughts feed at runtime. Do not list invented posts.

Write README.md that states what this is (a reading surface for the live feed), how to run (npm install, npm run dev), and that dergigi.com remains the only writing workflow. Point at the live RSS URL. Do not add a contributing guide that invites editing posts here.

Executor commits use Conventional Commits (D-11). After this task the repo documents that habit; do not add a commitlint package unless already present.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.version!=='0.1.0') process.exit(1)"</automated>
    <automated>test "$(grep -c '0.1.0' CHANGELOG.md)" -ge 1</automated>
    <automated>test "$(grep -c 'Keep a Changelog' CHANGELOG.md)" -ge 1</automated>
    <automated>test "$(grep -c 'npm run dev' README.md)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - package.json version is 0.1.0 (D-11, REPO-01).
    - CHANGELOG.md uses Keep a Changelog and records 0.1.0 (D-11, REPO-01).
    - README.md documents npm run dev against the live feed (REPO-01).
  </acceptance_criteria>
  <done>0.1.0, changelog, and a README that tells a human how to run the skeleton.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → dergigi.com RSS | Untrusted XML crosses into the client. CORS is `*` today. |
| Parsed description → DOM | Skeleton uses textContent, so markup is not executed. Plan 01-02 opens the HTML sink. |
| npm install → node_modules | Official create-vite packages only in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information disclosure | fetchFeed | low | accept | Feed is already public. No credentials. |
| T-01-02 | Tampering | parseRss item fields | medium | mitigate | Title, date, and permalink are text nodes (D-03/D-06). Description in this plan is textContent only; HTML insert waits for DOMPurify in 01-02. |
| T-01-03 | Denial of service | live feed size | low | accept | One author's log. Empty/failure path exists as RiverState.empty. |
| T-01-04 | Elevation of privilege | n/a | low | accept | No auth, no server. |
| T-01-SC | Tampering | npm create-vite / npm install | high | mitigate | Install only the official vanilla-ts template packages. No extra production dependencies in this plan. STACK.md has no formal Package Legitimacy table; treat template packages as the locked D-10 stack, not an open shopping list. |

ASVS L1. Block on high. T-01-SC is mitigated by refusing non-template packages. XSS from feed HTML is in scope for 01-02 (high, must mitigate there).
</threat_model>

## Artifacts this phase produces

Symbols and files created by this plan (exclude from source-grounding drift):

- `FEED_URL`, `PUBLISHER_ORIGIN`, `FALLBACK_FEED_URL` in `src/config.ts`
- `Piece`, `RiverState`, `LoadError` / empty reasons in `src/feed/types.ts`
- `fetchFeed` in `src/feed/fetchFeed.ts`
- `parseRss` in `src/feed/parseRss.ts`
- `loadRiver` in `src/feed/loadRiver.ts`
- `render` in `src/ui/render.ts`
- river / piece / loading views in `src/ui/river.ts`, `src/ui/piece.ts`, `src/ui/loading.ts`
- Vite app shell: `index.html`, `src/main.ts`, `vite.config.ts`, `package.json` scripts `dev` `build` `preview`
- `README.md`, `CHANGELOG.md`

<verification>
npm run build succeeds. FEED_URL is referenced from fetchFeed. Vite config does not fetch. README documents npm run dev. Version is 0.1.0.
</verification>

<success_criteria>
A visitor can run npm run dev and read live feed pieces, newest first, each linking to dergigi.com. The build does not snapshot the feed. Repo conventions for 0.1.0 exist.
</success_criteria>

<output>
Create `.planning/phases/01-live-reading-river/01-01-SUMMARY.md` when done
</output>
